Description,Target,Expression
Sum of Workers Auto Savings Ratio,hh_work_auto_savings_ratio,persons.work_auto_savings_ratio.groupby(persons.household_id).sum().reindex(households.index).fillna(0.0)
Sum of Worker Rail Proportion,workers_rail_proportion,persons.worker_rail_proportion.groupby(persons.household_id).sum().reindex(households.index).fillna(0.0)
Sum of School Rail Proportion,students_rail_proportion,persons.school_rail_proportion.groupby(persons.household_id).sum().reindex(households.index).fillna(0.0)